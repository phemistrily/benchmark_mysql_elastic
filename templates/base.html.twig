<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>">
        {# Run `composer require symfony/webpack-encore-bundle` to start using Symfony UX #}
        {% block stylesheets %}
            {{ encore_entry_link_tags('app') }}
        {% endblock %}

        {% block javascripts %}
            {{ encore_entry_script_tags('app') }}
        {% endblock %}
    </head>
    <body>
        <a href="{{ path('index') }}">SELECT - Dane nie zoptymalizowane</a><br />
        <a href="{{ path('index_elastic') }}">SELECT - Dane zoptymalizowane</a><br />
        <a href="{{ path('index') }}">INSERT</a><br />
        <a href="{{ path('index') }}">UPDATE</a><br />
        <a href="{{ path('index') }}">DELETE</a><br />
        <a href="{{ path('index') }}">TODO</a><br />
        {% block body %}{% endblock %}
    </body>
</html>
