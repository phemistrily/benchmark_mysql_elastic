<form method="post" action="{{ path('library_delete', {'id': library.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ library.id) }}">
    <button class="btn">Delete</button>
</form>
