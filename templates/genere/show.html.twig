{% extends 'base.html.twig' %}

{% block title %}Genere{% endblock %}

{% block body %}
    <h1>Genere</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ genere.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ genere.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('genere_index') }}">back to list</a>

    <a href="{{ path('genere_edit', {'id': genere.id}) }}">edit</a>

    {{ include('genere/_delete_form.html.twig') }}
{% endblock %}
