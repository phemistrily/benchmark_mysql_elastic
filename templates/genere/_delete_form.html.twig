<form method="post" action="{{ path('genere_delete', {'id': genere.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ genere.id) }}">
    <button class="btn">Delete</button>
</form>
